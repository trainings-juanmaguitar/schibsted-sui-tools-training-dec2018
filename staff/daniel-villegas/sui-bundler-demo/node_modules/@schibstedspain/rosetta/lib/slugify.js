// part of the code extracted from. https://github.com/epeli/underscore.string/blob/master/cleanDiacritics.js
'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
var from = 'ąàáäâãåæăćčĉęèéëêĝĥìíïîĵłľńňòóöőôõðøśșşšŝťțţŭùúüűûñÿýçżźž';
var to = 'aaaaaaaaaccceeeeeghiiiijllnnoooooooossssstttuuuuuunyyczzz';
to = to.split('');

function replaceCharIfNeeded(char) {
  var index = from.indexOf(char);
  return index === -1 ? char : to[index];
}

exports['default'] = function (str) {
  return str.toLowerCase().replace(/.{1}/g, replaceCharIfNeeded).replace(/[^a-z0-9 -]/g, '') // remove invalid chars
  .replace(/\s+/g, '-') // collapse whitespace and replace by -
  .replace(/-+/g, '-'); // collapse dashes
};

module.exports = exports['default'];