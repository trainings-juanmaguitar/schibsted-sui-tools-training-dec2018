import { UseCase } from '@s-ui/domain'

class ListStudentUseCases extends UseCase {
    constructor(respository) {
        super()
        this._repository = repository
    }
    execute() {
        
    }
}

export default ListStudentUseCases